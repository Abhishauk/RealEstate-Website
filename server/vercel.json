{
  "version": 2,
  "name": "node-js",
  "builds": [
    { "src": "server/index.js", "use": "@vercel/node" }
  ],
  "buildCommand": "npm install && npm run build",
  "routes": [
    { "src": "/(.*)", "dest": "server/index.js" }
  ]
}
